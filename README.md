# Lab6-Version-Control

The jupyter program takes in a name and an offset number, and shifts each character in the name over by that offset. Later letters will wrap around back to 'a' if they go over.
